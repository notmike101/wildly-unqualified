# Wildly Unqualified project instructions

## Version control and backup

- This repository contains only Wildly Unqualified. Do not add the parent research, other games or generic tooling. The user authorized private `notmike101/wildly-unqualified` on GitHub, frequent commits between coherent changes and a commit at every milestone. Keep the repository private. This supersedes historical no-Git/no-commit instructions in completed prototype/MVP plans.
- Preserve original Blender scenes and existing assets. Use new versioned source/export paths for additions.
- Commit the files belonging to each verified change; inspect staged scope and `git diff --cached --check`. Do not include another worker's unfinished changes.
- Push milestone commits and active feature-branch checkpoints to the private origin. Verify repository privacy and remote/local commit equality before reporting a backup. Do not force-push or rewrite published history unless separately requested.
- Keep room credentials, live saves, browser profiles, environment secrets, local test artifacts, dependencies and generated builds out of Git. Private game progress needs a separate backup of the complete private data directory.

## Development

- Execute the approved generated-reserves design and its implementation plan without repeated continuation questions. Resolve routine implementation issues and record material decisions in the progress ledger.
- Preserve the delivered MVP's portable release/data folder while developing the expansion.
- Asset authoring uses visible foreground Blender. Browser gameplay tests use visible browsers; close only owned test windows/processes. Claim Blender MCP use only after exercising a real callable connection.
- Prefer native/platform features and existing dependencies. Preserve server authority, frozen photographs, collision consistency, safe saves and portable self-hosting.
- Use CodeGraph before locating code if `.codegraph/` exists; otherwise use `rg`. Do not create an index as a side effect.
- Retrieve current library/API/CLI documentation with `ctx7` when required by the user's Context7 instructions. Do not put private code or credentials into queries.
