# Final documentation and release handoff

Read current implementation at the final accepted head before editing. These are concrete stale baseline references observed at1a9ff10, not a request to claim future gates now.

- README.md currently leads with ready forest-mvp-1, three species/four goals, schema-2-era gameplay and the old111-test result. Preserve those historical results in the old checkpoint; the final expansion introduction should state actual delivered12 species/36–48 residents, generated6+2 goals and implemented40–60-minute target with human pacing clearly unverified. Link the new checkpoint and accurate launch record.
- README.md documents test:browser as hardcoded4316, and the old driver still needs Task8 migration. Document the actual configurable owned port/seed/output flags after implementing them; never instruct users to stop their preserved room to run a test that can use a fresh port.
- README.md prototype/local/MVP private-access links and ../../research paths are historical local workspace links, not portable repository resources. Keep game-only scope: do not copy research or private access files into Git. Clearly mark preserved local artifacts/history and provide final local access links privately at delivery.
- SERVER.md currently identifies forest-mvp-1/schema2, seven runtime modules and selected four commissions. Currentrelease.ts already packages forest-expedition-1/schema3 and eight runtime modules includingworld.ts; rederive the final allowlist after Tasks5–7. Update exact state/retention/archive/next-reserve behavior and incompatible-save refusal from verified code.
- Keep the existing deployment contract: Node26 withinmajor26, exact publicOrigin/Origin check, separate private directory, absolute-entrypoint support, onlyonewriter, complete private-directory copy with room credentials, sessions re-admitted on changedorigin. Home-serverOS/architecture is inaccessible and must remainunverified.
- Task6 must supply the precise archive layout/digest/retry/failure and durable-publish semantics plus Windows directory-sync limits. Describe ordinary storage failure and retry without promising power-loss durability beyond evidence. Never replace original private data or silently convert schema1/2.
- VERIFICATION.md should lead with the new observed gate, keep old actualresults clearly historical, and distinguish isolated rule tests, realJPEGs, temporary injectednetworkfaults, actualHTTPS/WSS+150ms, displaypacedframe intervals, humanfun/duration/listening and inaccessiblehome server.
- Audio credits/provenance/license files must be verified in the portable web payload. Actual36decoded assets19.37MiB and .95ceiling are previous module evidence; finalintegratedloop/cue/listening evidence must be labeled separately. Useraudiofeedback remains unanswered.
- Recheckpreserved-mvp-files.json at thefinalgate. The old liveMVP4316PID23068 and its oldrelease/data remain untouched. Close only ownedtestprocesses.
