# Visible acceptance driver handoff (controller at110957c)

Current browser.ts already uses separate headed Edge launches with distinct contexts, ordinary keyboard/button inputs, read-only window.wildly state, tiled windows, real WebSocketRoute latency, JPEG persistence, restart/reconnect and owned cleanup. Reuse those mechanics; do not add another test framework or a debug command channel.

- Source copies currently use resolve("wildly-unqualified", file), wrong when invoked from the game repository root. Resolve from import.meta.url consistently, and add world.ts to frozen runtime imports.
- Port4316 is the preserved live MVP. Use an explicit free expansion-test port/options and separate private output/data directory; real HTTPS origin points to that owned listener only. Never repurpose or stop the old tunnel/listener.
- Replace WALLS/WALKABLES/NAV_NODES/HABITAT_SITES and one route boolean with the installed blueprint and actual fixture helpers. The existing short local A* detour costs up to3000 expansions; use authored generated navigation for long travel and local collision-aware detours only near the destination. Large-world paths must use blueprint camera/anchor/handling approaches.
- Existing scenario is hardcoded to three legacy species and one tin escort. New scenarios should read commissions/prerequisites and rotate four roles between two useful local tasks. A shared tin is not justification for sending everyone on a single escort across the map. Track unavoidable idle intervals separately from intentional animal observation.
- Existing frame metrics are final snapshots only; collect moving dense-view frame intervals, renderer calls/triangles, network payloads and server tick timings while four players occupy distant regions with full population. Preserve the same viewport/hardware for baseline comparison. A percentile alone needs its sample window/count.
- Preserve the real +/-latency/2 socket delay for the required HTTPS/WSS run; verify actual RTT. A local fake socket is not an internet-path acceptance run.
- Read snapshot via diagnostics, world via a separate immutable diagnostic if needed. Never write internal state, invoke privileged commands, teleport, force an animal behavior, inject JPEG success or grant credits. Normal buttons/keys and authentic issued-cookie restoration only.
- Current photo output assumes /^photo-\d+$/; change to the production world namespace while protecting output paths. On restart, compare world hash and actual decoded JPEG bytes, favorites, supplies and retained wildlife memory. Next-reserve is a host UI action, with a different outing ID and geometry.
- Failure evidence and private storageState/run snapshots remain ignored; do not log credentials. Capture screenshots/JPEGs for all twelve species across legitimate encounter coverage. Node elapsed time is orchestration time, not human pacing.
- The old driver owns and closes its created browsers/server in finally. Preserve those ownership boundaries. If a user asks to keep an active live playtest open, distinguish it from temporary acceptance windows.

## Measurement preparation inspected at frozen c3c5957

- Server startServer currently chooses a random seed and returns only url/close. The controlled maximum48 performance case can use a fresh untouched createRun(4891, newIdentity) serialized through actual saveRun before anybody joins. Label that seeded initial-world fixture separately from naturally selected full outings; never alter player/animal positions, credits or established saves. Reverify the resulting resident count with final world code. No new production seed configuration is needed solely for this benchmark.
- Existing server interval advances fixed1/60 steps and broadcasts each sixth step. Snapshot tick progression plus wall time can establish achieved step rate and stalls; it cannot establish per-step CPU percentiles. Native process memory/CPU and event-loop sampling or explicitly labeled temporary instrumentation are separate measurements. Do not substitute client rollingp95 for server timing.
- Existing window.wildly getter clones the snapshot and sorts frameTimes. Match the baseline's500ms diagnostic samples, collect raw RAF timestamps directly, and precompute collision-valid movement paths before the timed window. The baseline window was30.008s/3601frames/53.45m at1280x720,DPR1,NVIDIAAmpereWebGPU; retain hardware/viewport evidence. Report geometry/texture counters as renderer estimates, not measured VRAM.
- Current world.test.ts already covers258 distinct sweepseeds plus fulluint32 edge comparisons, all12 required-species pool, population/density/crown/connectedgraph limits and actual support/route checks. Do not add a duplicate256-seed test; run the settled existing suite once after final source. Broader actual ordinary goal accessibility and two-world isolation remain separate acceptance evidence.
