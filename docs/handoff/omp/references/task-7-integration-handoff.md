# Task 7 integration handoff

Read-only source mapping against accepted `aa4d9414746a0da5c0e9d1ca270a0392b5bdac10`; line numbers below refer to that frozen commit, read with `git show`. Current Task 5 working changes are not audited here. The approved native limiter/water-fade repair is already integrated: preserve it. No source edits, tests, browser launches or listening occurred in this preparation.

## Existing producers → native input

| Source at aa4d941 | Actual available fields / minimal integration |
| --- | --- |
| `server.ts:242` cue delivery; `253` emitCues; `shared.ts:223` wire union | Current cue is `{worldId,id,kind,source,position}`; kind is whistle/rattle/shutter/impact/alert. Preserve world identity, exact event-time position and authoritative dedup. Add optional `species` and `material` to this existing message, plus only the additional kinds actually emitted. No second audio protocol. |
| `game.ts:1122,1129` rattle/whistle; `server.ts:783` eventCueKind | Direct native `rattle` / `whistle`, existing world-scoped ID and position. Rattle already chooses metal-pot variants; no extra material field needed. |
| `server.ts:272–282` entering alert | Exact resident ID, `animal.species` and pose are already available at source; transmit species. Native `alert` without species selects no sample. Keep resident ID as source, not species-as-identity. Seeded alerting/heard sets at server startup (`190–200`) intentionally avoid restore replay. |
| Task 5 animal clue/action producers | Use the same message with authoritative resident ID, species, event-time position, and stable world-scoped event ID. Native `wildlife` / `action` select `wildlife-${species}`; `rustle`, `splash`, `tap`, `gnaw` select specific foley. Do not generate calls every snapshot/frame or infer an animal from a nearby placement. Agreement sent directly to outing_driver. |
| `physics.ts:39,63,350–369`; `game.ts:1491–1498` | Native contacts already carry `sources: string[]` identifying tin and real prop IDs. Game currently discards that identity when recording `noise`; `event(..., null, ...)` at `game.ts:152` stores the legacy player value `tin`, even for a wooden prop contact. Derive an optional audio material from these actual involved bodies before identity is lost. Tin → metal; known wooden equipment kinds → wood; unknown contact → generic impact. Existing tags do not identify static terrain material; do not claim they do. |
| `game.ts:1418–1425` carried-prop collision | The exact `prop` is available when impact/noise is emitted; its definition/kind can supply material. Preserve existing player attribution and gameplay noise semantics. Do not change legacy `player` to fix an audio classification. |
| `main.ts:486–501` footsteps | Existing local cadence uses authoritative consecutive player displacement, resets on pause/large correction, but supplies no material. Keep this bounded cadence and create world/local-player/tick-scoped step IDs. Classify the actual supporting surface at the feet, not the nearest decorative model. |
| `server.ts:639–647` shutter | The fresh accepted photo generates a shutter cue with photographer source and frame ID. Native shutter is deliberately unpanned/local (`audio.ts:396`): send it to native audio only when `source === localId`. Teammate shutter should produce a visible named caption. Do not also sound it in pending-frame restoration/queuePhoto; reconnect sends old photo frames but no fresh shutter cue. |

The minimum metadata agreement is therefore **existing cue envelope + optional species/material**, not new state snapshots or an audio manager. Prefer concrete species typing from the shared animal type and a small material union (`wood`, `metal`, `trail`, `wet`; native also accepts tin/water aliases). If persistent `RunState.events` gains optional metadata, update its strict save validator deliberately; avoid leaking transient sound queues into saves. Actual contact IDs may remain server-side when only material is needed by the client. Generic impact is preferable to invented contact specificity.

## Listener, environment and attenuation

- Replace both old `main.ts` playSound call sites (`415`, `495`) and remove `view.ts:795` oscillator implementation/import. In particular remove `Math.max(0.05, 1-d/24)`. Native effects already have finite HRTF linear attenuation (32 m; footsteps 12 m), and water already fades over 18 m. Supply no second distance gain. Existing server fan-out is 22 m / shutter 12 m: retain/document that authoritative delivery cutoff or deliberately align it to the intended range; it is a range gate, not a gain multiplier.
- In the existing animation loop (`main.ts:1120`), immediately after the actual predicted camera assignment (`1211–1212`), call listener with camera position and `forward(yaw,pitch)` (shared.ts:417). This matches the rendered camera, including crouch eye height and mouse look. Do not use lagging latest snapshot yaw/position, remote interpolated animal orientation or photo-render camera transforms (`queuePhoto` temporarily renders frozen frames).
- `world.ts:83–107` already has authored pocket habitat, density regions and exact water boxes. Use actual enclosing density/habitat geometry where defined, with woodland default between pockets; nearest pocket is only a labeled approximation because pockets have points, not habitat extents. Water distance should be minimum distance to the water surface rectangle (clamp listener X/Z to min/max, use surface maxY), not distance to a pond center or arbitrary water model. Pass Infinity when absent. Native uses that distance for the quiet nonpositional water bed; event splashes remain positional.
- For steps, reuse `surfaceHeight` (`shared.ts:522`) and the same candidate set as `predictStep` (`main.ts:232–245`): world.walkables plus currently open fixtureSurfaces. Choose the actual highest supported surface matching foot Y. A seated crossing deck is wood even above water; standing by a pond on dry ground is not a wet step. Classify wet only from actual supporting/contact geometry; otherwise trail is the honest generic fallback. No new general spatial/material abstraction is needed.

## Settings and single-instance lifecycle

- Instantiate createAudio once for the client, outside createView/installWorld/openSocket. `audio.ts:323` unlock is idempotent, owns its one context and bounded cache; the module already owns document visibility pause/resume and dedup even for discarded muted/paused cues.
- Extend existing `wu-settings` load/default/save (`main.ts:83–110,193,1307–1316`) with effects/ambience/music while preserving `volume` as master, including exact zero. Use native defaults for missing new fields (effects .8, ambience .35, music 0); validate finite values. Labels belong in existing Settings UI (`index.html:144–169`). Captions stay enabled at master zero and when AudioContext is unsupported.
- Call unlock synchronously from the Join submit gesture before network awaits (`main.ts:1234`), Continue/resume button (`1273`), and existing Enter controls/viewport gesture (`271,1264`) so restored sessions that bypass Join still have a gesture path. Do not await audio readiness to admit players. Repeated gestures reuse the same module.
- Pause the module while no installed/ready world or connected current snapshot exists, and whenever the room is paused. Pause immediately on installWorld reset (`327`) and socket close (`433`); unpause only after coherent current-world receive (`470`) confirms the desired state. Use the existing instance through retry/backoff; never start another loop per reconnect. Reset stepDistance on world install/connection loss so pre-reconnect partial strides cannot create a stale step.
- Keep current worldReady/worldId/heard checks (`main.ts:407`) before cue dispatch; maintain captions separately from audio success. Module visibility handler (`audio.ts:271`) already stops voices; main's hidden-page handler (`1368`) neutralizes input. Do not introduce a second visibility-owned audio loop. Dispose once on terminal leave/page teardown; handle persisted pagehide separately if supporting back/forward cache, rather than leaving a disposed instance attached to a resumed page.
- Static delivery is still blocked by `server.ts:52` MIME allowlist for the delivered .wav/.ogg files. Add those actual extensions; JSON only if manifest/provenance is fetched directly at runtime (the current module imports manifest into the bundle). Keep release audio files/provenance available locally.

Remaining acceptance is Task 7's ordinary visible/listening work: actual rendered listener orientation and range, named teammate shutter captions, surface/contact-specific samples, silent mute, one loop set after pause/hidden/reconnect, native asset delivery and sustained listening. The existing module output proofs are not repeated or upgraded into in-game listening evidence here. Task 6's prepared retention test remains untouched and paused.
