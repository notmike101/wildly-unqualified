# Return to the orchestrator

Create results/FINAL.md, update CURRENT.md, commit/push, verify remote SHA/privacy, then stop. Report at most one screen of user-facing text with links. No automatic main merge or final tag.

Include:
1. Exact final branch, worktree, base/source HEAD and remote HEAD; all sequential task branch/SHAs (or link results/NN.md).
2. Packet completion table: implemented / verified / blocked. Distinguish product defect, obsolete fixture, test-driver defect and external limitation.
3. Final commands and counts: full suite, TypeScript/build, formatting, GLB checks, audio asset check, portable install; link raw ignored logs.
4. Two-player and two distinct four-player command runs; required IDs earned, actual JPEG evidence, equipment/mischief/recovery, exhibition, restart and next world. Identify which checks used actual sockets/browser/HTTPS/WSS/150 ms and which used in-process simulation.
5. All-12-species image/contact/motion results and any actual Blender edits (source/export paths, MCP proof, foreground confirmation).
6. Measured seed/world/payload/performance limits with hardware, viewport, sample duration/count and instrumentation overhead. Do not present display-paced RAF intervals as CPU/GPU execution time.
7. Album/archives transaction guards, immutable retry identity, crash points, pre/postcommit outcomes, private-directory relocation and preserved-original hash comparison.
8. Native audio controls/output/asset/caption results and exactly who listened. Human pacing/fun, unavailable listening and inaccessible home server remain explicit gates.
9. Candidate release directory/hash manifest, private local launch instructions path, owned process IDs/ports and cleanup status. Never include secrets or public invite fragments in versioned output.
10. Remaining risks, the exact next reproduction command, and what the orchestrator should review before main integration.

Completion criteria: all feasible required automated gates pass on the final settled source; actual delivered behavior matches STATE.md; no unexplained failure or missing data; candidate remains private/portable. If blocked, name the smallest missing input/capability and preserve an honest checkpoint. Do not claim completion solely because the context window or allotted time is ending.
