# Preserved drafts — not approved implementation

These copies freeze pre-pause local work from the original routines checkout at committed source b16f00e. MANIFEST.json records SHA-256 and original paths. Originals are preserved and must not be edited, reset or cleaned by the receiving worker.

- encounters.ts: includes one tentative six-line unchanged-decoy stability guard. UNVERIFIED. Use packet 01 to reproduce and test root cause; inspect only its small diff against b16.
- encounters.test.ts: bounded wash/Deer precondition waits and stronger investigation-stability assertion. Partial work, not a green suite.
- expedition-equipment.test.ts and expedition-equipment-helpers.ts: helper extraction for reuse; selectively adapt in packet 04.
- expedition-validation.ts: incomplete accelerated loop/species producer. Seed-6 loop stalled at camera guidance; Raccoon setup omitted required close approach; some photos name neighbors or are too small. It is not acceptance proof.
- render/client.ts and run.mjs: historical production renderer, with old checkout-relative paths, fixed port and polling. It made 15 JPEGs; visual inspection was incomplete. Adapt to current source and finite inputs in packet 05. Historical STOP/output/cookie files are deliberately excluded.

Do not run drafts directly: imports refer to missing game modules at this location. Copy only the needed draft into the current task checkout, resolve imports/arguments, verify it, then commit it as actual test code. Do not wholesale overwrite current source with an old full-file draft.

The original ignored logs/images/JSON frames remain at:
D:/friendslop-games/games/wildly-unqualified/.worktrees/expedition/.superpowers/sdd/2026-09-06-generated-reserves

These are historical evidence. Generate new evidence in your own .artifacts/omp/NN directory; never replace old failure output with new passing output.
