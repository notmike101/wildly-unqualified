# Observed Wildly Unqualified verification

> Historical record preserved during the 2026-09-07 reorganization. Commands,
> source paths, process status, and acceptance claims describe that earlier version.
> See [the current maintenance guide](../MAINTAINING.md) and [server guide](../SERVER.md).

Recorded 2026-09-05. This is a local technical prototype with an authored linked outing. **The intended 20–30-minute human session and separate-internet human cooperation are not measured results.** The larger expedition and all earlier concepts remain preserved.

## Current forest MVP acceptance

The [forest checkpoint](MVP-CHECKPOINT-2026-09-05.md) is current. Independent equipment, wildlife, mischief, photograph/exhibition and asset reviews pass. Fresh checks pass **111/111 game tests**, **3/3 shared tests**, game/shared TypeScript/build and formatting. The delivered client is `index-C-4DfThH.js` with `index-yUq1JVPQ.css`; the existing bundle-size advisory remains recorded. [Exact gate outputs](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/final-gates.md).

- Ordinary-controls two-player and four-player 150ms equipment routes pass: shared case, gate, physical plank placement and every player's crossing, with zero unexpected browser errors.
- One visible two-player outing and two distinct four-player seeds completed all four selected commissions, shared actual 640×360 JPEGs, returned everyone to camp, readied, finished the exhibition and retained a favorite through restart/reconnect. These were saved-world continuations after recorded driver failures; the two-player chain also includes a compatible tipped-equipment recovery correction. They are not uninterrupted runs or measurements of human playtime. Spill, hat and moved-decoy recovery passed through ordinary controls. An original JPEG shows a raccoon wearing the borrowed yellow hat; the final combined portrait has both subjects separately visible, although small in frame. [Outing evidence and retained unsuccessful attempts](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/outing-report.md), [corrected machine-readable acceptance](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/outing-acceptance-summary.json).
- All five new GLBs validate without errors/warnings; actual imports verify 39 models and 89 pivots. The world contains 718 placements, including 179 mature trees. A discovered dry-bank edge mismatch now passes imported-mesh, production-walking and native-tin rechecks. [Final static asset audit](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/task-7-final-assets.md).
- All 18 protected baseline asset/save files are unchanged. A separate fresh check confirms the original 21-file portable release and the accepted private prototype outing retain their exact SHA-256 hashes. [Release/save preservation](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/preservation-release-final.json).
- The final clean 27-file / 5,343,156-byte release exactly matches a separate three-package production install. Started from the system Temp working directory, it preserved the stopped browser outing's earned raccoon commission and all three JPEGs, added a favorite through an authenticated socket, saved and copied the complete private directory, then recovered the host and a new guest on a new port/origin/data path. Exact original image bytes, favorites, selected world, animal state, supplies and resumed simulation pass. Original source data hashes are unchanged; owned ports 4317/4318 closed. This uses an interrupted two-player outing; full-slot guest reassignment is tested separately in the socket suite. [Final release relocation result](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/portable-final-1788666210947/result.json), [final clean/tested payload hashes](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/portable-release-final-manifest.json).
- Actual visible camp, woodland, clearing, wetland and sky views pass, together with trunk/log/rock blocking and a real JPEG through the standing screen opening. A 20-second active 1400×820 viewport sample on RTX 3080 measured median 8.30 ms / p95 9.00 ms across 2,401 frame intervals and stable 145.67 MiB estimated renderer memory. A labeled stationary authenticated socket fixture kept the outing active; it was not a hidden browser. This is presentation cadence on this PC, not GPU timestamp timing or a minimum hardware specification. The owned browser, fixture and server were closed. [Final forest/performance report](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/task-7-visual-performance-report.md).

- Final original pixels verify four crew colors, names, numbers and owned hat silhouettes in shade and direct sunlight. The wash photo exposed a visual defect: raised trail slabs covered the shallow brook. Lowering only the rendered trail surface passes a production-mesh raycast regression and actual live/JPEG inspection. The final sun comparison uses the delivered client. [Final visual supplement](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/final-visual-supplement-report.md), [crew comparison](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/task-2-crew-visual-acceptance.md).
- A final visible browser on the delivered build restored the completed HTTPS outing and decoded all six photographs at 640×360 through ordinary notebook scrolling. Removing the pale favorite-label override gives dark, readable attribution; actual computed colors and screenshot pass. Its browser/server closed and original data stayed unchanged. [Final notebook check](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/final-notebook-1788666415701/result.json).
- A fresh paused room is running on this PC with zero reserved slots, no commissions and no photos. Public/local health, exact public bundle/CSS/forest hashes and rejection of unauthenticated/private paths pass. Only the new MVP and preserved prototype servers remain on their intended ports; owned browser test processes are gone and Blender remains open. [Secret-free launch record](../../../.artifacts/wildly-unqualified/mvp-launch.json), [private host access](../../../.artifacts/wildly-unqualified/MVP-HOST-ACCESS.md).

| Completed visible outing chain | Seed | Selected alternatives | Original JPEGs |
|---|---:|---|---:|
| Two players, loopback | 1150906316 | Inspect, graze, display, combined portrait | 5 |
| Four players, loopback | 3939270088 | Inspect, graze, preen, combined portrait | 7 |
| Four players, HTTPS/WSS + 150 ms added delay | 892274019 | Wash, decoy interest, preen, combined portrait | 6 |

The internet test adds 75 ms in each direction to real WebSocket traffic. Active screenshots show 207–234 ms total input-acknowledgment RTT. Final post-restart samples were 1165–1401 ms and can include old pending input timestamps; they are not a measurement of the configured added delay. One screenshot initially read as 23 ms actually shows 234 ms. [Transport review](../../../.artifacts/wildly-unqualified/mvp-2026-09-05/latency-reconnect-review.md).

The preserved raw internet result inherits incorrect pre-deadline hat wording: the hat actually timed out onto reachable ground and was retrieved with E. Its 178.7-second decoy recovery includes manual driver guidance around the prop, and two early DOM image sizes were sampled before decoding. The acceptance summary records these corrections without overwriting raw results; the final notebook check independently closes the image-loading evidence gap.

During four-player photography, two agents usually handled setup/camera work while two waited as observers. The predominant strategy remained tin escort and quiet bait/photo timing, even with the alternate commissions. Equal contribution and broader human strategies have not been demonstrated. The active performance sample predates the final three-centimetre trail lowering and text-color change; those final builds received visual/bundle checks, not another performance benchmark.

These results do not establish human duration, enjoyment, equal four-player engagement, or people on separate networks. The historical prototype metrics below refer to its old scene and three-goal loop and must not be reused as current forest performance.

## Preserved prototype evidence

## Later visible four-player run

The user accepted the subsequent rough-prototype run as successful and requested a more varied, polished MVP. Four visible independent browser profiles, controlled by the coordinator and three subagents, completed all three photo assignments, shared four actual JPEGs and returned to camp. The first logged all-assignment completion was at 466.8 simulation seconds (7:47); the host's return was logged at 588.4 seconds (9:48). This guided run does not measure unfamiliar human playtime. Browser-owner interruption and a later Computer Use policy stop prevented verification of its exhibition; the earlier automated exhibition result below remains separate evidence.

The [full playtest record](PLAYTEST-2026-09-05.md) preserves actions, screenshots, interruptions and the user's six observations. Source inspection confirms sparse collision coverage, fixed animal destinations, absent overhead names and approximate tree placement. Outfit tint code already exists but did not provide sufficient visual identification. All four identified player browsers were subsequently closed; their control-port listeners were confirmed gone. Other windows were left open.

The [MVP design](../superpowers/specs/2026-09-05-wildly-unqualified-mvp-design.md) was approved with the user's mature-forest correction. The current acceptance section above supersedes that early implementation checkpoint. The archived prototype results below remain separate evidence.

## Preserved prototype result

| Area | Status | Observed evidence |
|---|---|---|
| Game rules, ownership and real physics | VERIFIED | 30 game/rule/geometry/WASM/save/socket/release checks pass |
| Browser gameplay and actual photographs | VERIFIED locally | Two actual browser controllers complete all three photo assignments, return to camp and enter the exhibition; four independently authenticated contexts share state and JPEGs |
| Asset import and coherence | VERIFIED technically | Nine original GLBs validate without warnings/errors; actual GLTFLoader hashes, bounds, hierarchies and 103 pivots pass; source render and in-game views inspected |
| Local hosting | VERIFIED | Latest compiled game served by Node on `127.0.0.1:4310`; health reports build `prototype-1`, schema `1` |
| Portable installation and local relocation | VERIFIED locally | Fresh production-only install, different working directory, port/origin/data transfer and preserved rule-earned photo |
| Public HTTPS/WSS transport | VERIFIED from this PC | Two isolated browsers share movement, cues, actual JPEG and rejoin through the tunnel; the subsequent four-player outing is preserved in that private-admission room |
| Human duration, understanding and enjoyment | IMPLEMENTED BUT UNVERIFIED | No unfamiliar two-person or four-person session has been conducted; bots do not establish fun or duration |
| Inaccessible home-server OS/architecture | UNVERIFIED | Local relocation establishes the configurable packaging/data contract, not operation on that machine |

## Reproduction and checks

These were the prototype checkout commands. The source now targets the forest MVP, so use the [current checkpoint](MVP-CHECKPOINT-2026-09-05.md) for development and the preserved release for version-1 data:

```powershell
npm.cmd run wu:test
npm.cmd run wu:build
npm.cmd run wu:format:check
npm.cmd run wu:test:browser
```

`wu:test` passes **30/30**, with no skipped tests. `wu:build` passes strict game TypeScript checking and Vite compilation. The final browser JavaScript is `index-q_kRS9hu.js`, 868.26 kB minified / 241.37 kB gzip. Vite reports its normal greater-than-500-kB chunk warning; it is not suppressed. Formatting passes.

The existing shared `npm test` (**3/3**), `npm run build`, `npm run format:check` and `npm run test:browser` also pass. The generic inspector still loads real WebGPU, runs the standard Box3D WASM, falls/contacts, exports/reloads, rejects invalid input without losing the scene, resets and disposes. No unrelated tool changes were required.

Full actual-controls route and injected WebSocket latency:

```powershell
$env:WU_FULL = '1'
$env:WU_LATENCY_MS = '150'
npm.cmd run wu:test:browser
Remove-Item Env:WU_FULL, Env:WU_LATENCY_MS
```

The test delays real messages by 75 ms in each direction through Playwright's connection to the actual server. It does not fabricate snapshots, award objectives, teleport players or add production test endpoints. The same tin is picked up, opened, inspected, stolen and reclaimed; the heron is fed and calmed; a second player photographs both species; both return and finish. The final rendered layout leaves the shelter opening clear of the decorative tripod.

The full route passed, including four-client admission, photo persistence, rejoin and server restart. A later optional performance extension initially raced a replacement socket against browser disconnection and was correctly refused with HTTP 409. The harness now waits for the authoritative disconnected slot. The subsequent ordinary browser/performance command passes; this was a test harness repair, not a relaxation of duplicate-session admission.

One-viewport performance check:

```powershell
$env:WU_PERF = '1'
npm.cmd run wu:test:browser
Remove-Item Env:WU_PERF
```

This completes the ordinary browser regressions, replaces three test pages with authenticated real sockets, resumes the outing and samples one rendered 1280×720 viewport for 20 seconds with four players represented in the world. The test does not confuse four rendering contexts with the one-viewport target.

## Measured browser results

Observed in Edge **152.0.4191.62**, real WebGPU backend, NVIDIA Ampere adapter, `isFallbackAdapter: false`. Windows also reports the physical NVIDIA GeForce RTX 3080. These are measurements on this PC, not minimum hardware specifications.

| Measurement | Result | Scope |
|---|---:|---|
| One rendered viewport p95 frame interval | **8.5 ms** | 20 seconds, active/unpaused outing, 1280×720, four represented players; below 33.3 ms target |
| Four rendered clients p95 sample | **25.7 ms** | Short local multi-context sample, separate from the one-viewport measurement |
| Local acknowledged-input RTT estimate | **34.6 ms** | Includes server snapshot cadence; not raw network ping |
| Injected-latency acknowledged-input RTT | **187.5 ms** | 150 ms added WebSocket round trip; includes snapshot cadence |
| Initial local WebGPU page readiness | **607 ms** | One warm local load, including browser page creation; not a cold internet download |
| Click to shared-photo-ready notification | **117 ms** local / **374 ms** delayed | Includes capture, upload and second-client ready metadata; actual JPEG decode and nonuniform pixels are separately asserted |
| Browser uncaught errors | **0** | Completed local browser runs |

Machine-readable evidence: [ordinary browser result](../../../.artifacts/wildly-unqualified/browser-result.json), [latency route result](../../../.artifacts/wildly-unqualified/browser-latency-result.json), [one-viewport result](../../../.artifacts/wildly-unqualified/one-viewport-result.json). Metrics are reproducible samples, not averages over a hardware population. Packet loss, network jitter, browser background suspension and every remote GPU have not been exhaustively tested.

Browser checks also cover an actual decoded shared JPEG, camera/viewfinder agreement at a tall 1100×900 window, prolonged arrow-key turning followed by valid movement, leaving without auto-rejoining, identity-preserving page reload, and a 3.5-second server outage followed by automatic recovery. The diagnostic browser property returns cloned/read-only observations rather than mutation controls.

## Persistence, roles and portable release

Real socket tests reject incorrect origins, missing/wrong credentials, a duplicate active session, oversized messages and excess slots. Saved data stays outside the static allowlist; traversal, source/private paths and escaping symlinks are refused. Photo upload is author-only and bounded to actual JPEG structure, 640×360 and 64 KiB. Uploaded pixels cannot award assignments.

Versioned saves retain images, immutable pending frames, timers and identities. Serialized atomic replacement keeps a valid backup. Tests cover failed writes, concurrent saves, corrupt primary recovery, unsupported versions, inconsistent references, actual impact events, and failed final flush remaining paused/repairable. Rejoining and reassigning a disconnected slot invalidates the old credential without allowing a connected player to be evicted. Whistle, rattle and shutter cues arrive once, with no historical replay after reconnect or restore.

The relocation test obtains `raccoon-inspect` through the actual `applyCommand(photo)` path. It preserves that frame and JPEG, two tin portions, one patch portion, full animal state and 123 simulation seconds; stops, copies the whole private directory, starts on a different actual port and origin, rejects the old origin, recovers the host, reassigns a new guest, reads the same image and resumes advancing simulation. Its starting animal pose is a unit/integration fixture; the separate browser route establishes completion through real controls.

The clean delivery is [prototype-1 portable 2026-09-05T20-45-09-720Z](../../../.artifacts/wildly-unqualified/releases/prototype-1%20portable%202026-09-05T20-45-09-720Z): **21 files / 2,786,631 bytes**. It contains six runtime TypeScript modules, the compiled web directory, the unchanged package/lock and server runbook. No installed packages, Blender sources, credentials or saves are included.

The [portable verification and SHA-256 inventory](../../../.artifacts/wildly-unqualified/verification/portable-release-final-1788641021435.json) records a separate tested installation with identical payload hashes. `npm ci --omit=dev` installed **three production packages**, audited four and reported **zero vulnerabilities**. The absolute Node entry point launched from the system temporary directory on **port 51044**, passed health and all 12 web asset checks, admitted two real WebSockets, delivered a shared whistle, saved paused state and exited **0**. Logs contained no credentials. The clean delivery's every file matches that tested payload.

The application server needs Node 26.5+ within major 26, its pinned runtime packages and writable storage/network access. It needs no GPU, Blender, Codex, Vite or Cloudflare API. A proxy supplies HTTPS/WSS where required. [SERVER.md](../SERVER.md) gives installation, origin handling, backup/restore, stop and migration steps. The home server remains untested because it is inaccessible.

## Original assets and visible Blender

The preserved version-1 CLI pass used actual background Blender. Following the user's correction, **every version-2 construction, export and render ran in visible Blender PID 51680**, with timers yielding between model batches. No Blender MCP calls were made. The source remains open on `library-v2.blend`; foreground handle `0x210BBE` was independently confirmed at 20:38:05 UTC. No unrelated scene was overwritten.

The additional library has 24 named environment/equipment models and 15,644 triangles. Four character/wildlife variants preserve their moving-part hierarchies; they are cosmetic additions to the existing two species. All character variants remain below 8,000 triangles. Actual integration includes tracks, notebook, nest and shelter clues, foliage variety and supported camp equipment. Wall strips preserve the shelter's viewing window and rear entrance.

```powershell
node wildly-unqualified/assets/check.mjs
node wildly-unqualified/assets/check.mjs wildly-unqualified/public/models wildly-unqualified/assets/manifest-v2.json
npm.cmd run validate:glb -- wildly-unqualified/public/models/researcher.glb wildly-unqualified/public/models/raccoon.glb wildly-unqualified/public/models/heron.glb wildly-unqualified/public/models/field-kit.glb wildly-unqualified/public/models/reserve-kit.glb wildly-unqualified/public/models/researcher-raincoat.glb wildly-unqualified/public/models/researcher-vest.glb wildly-unqualified/public/models/raccoon-dark.glb wildly-unqualified/public/models/heron-reed.glb
```

All nine actual imports and glTF validations pass, with **zero errors/warnings**. These checks use Node and do not launch background Blender. [Version-1 provenance](../../assets/PROVENANCE.md), [version-2 provenance and dimensions](../../assets/PROVENANCE-v2.md), [editable source](../../assets/library-v2.blend) and [actual Blender render](../../assets/preview-v2.png) are retained.

In-game evidence: [raccoon photograph](../../../.artifacts/wildly-unqualified/raccoon-photo.png), [clear shelter view with both animals](../../../.artifacts/wildly-unqualified/pond-pair.png), [shared album](../../../.artifacts/wildly-unqualified/shared-album.png) and [completed exhibition](../../../.artifacts/wildly-unqualified/exhibition.png). Actual captured images were decoded and visually inspected for upright geometry and the credited subjects. No separate calibrated color-chart comparison is claimed. Human unlabelled recognition and sound-usability checks remain pending.

## Review and remaining gates

An independent scoped code review found four issues: reconnects stopped on transient HTTP failures, keyboard yaw was unbounded, viewfinder framing disagreed with capture after resizing, and shared actions lacked audible cues for friends. These were corrected and passed the focused re-review and affected browser/socket checks. Local footsteps use received movement, including quieter crouching; shared important cues have source captions and volume control.

Human playtests must still measure actual duration, join friction, misunderstood animal reactions, idle periods, recovery effort, meaningful contributions, favorite photographs and desire for another outing. No volunteers were contacted. A local automated successful route does not establish a sustainable 20–30-minute experience, and extra models do not prove content hours.

The full expedition, third species, procedural content, integrated voice, mobile/controller support, Steam integration and store packaging are absent from the approved prototype. The new MVP proposal includes a third species and bounded encounter variation; it does not change these implementation results. Costs continue to use the existing $100 individual subscription, free/open-source tools and owner time; no paid runtime AI or outsourced labor was introduced. No purchases, store/repository publication, commits or pushes occurred.

## Actual internet transport and current access

After renewed user launch authorization, the installed Cloudflare Quick Tunnel started through a normal task-managed process. Two isolated Edge browser contexts on this PC loaded actual HTTPS assets and authenticated WSS at the assigned origin. The second browser observed 2.85 m of keyboard-driven movement, one whistle and a rendered shared photograph. The 640×360 JPEG was 26,004 bytes, arrived in 182 ms and decoded with a red-channel range of 195; visual inspection confirmed actual scene geometry. Disconnect/rejoin preserved the same slot and album. No page/asset errors occurred.

Five WebSocket ping/pong RTT samples through the tunnel were 24.7, 26.5, 25.1, 27.6 and 26.5 ms: **26.5 ms median**. The game acknowledgment estimate was 56.4 ms. Wrong HTTP and WebSocket origins returned 403; an unauthenticated WebSocket returned 401. Session cookies were Secure, HttpOnly and SameSite=Strict. These are actual transport measurements from this PC, **not** two humans on different networks.

Evidence: [transport result](../../../.artifacts/wildly-unqualified/internet-test-2026-09-05T20-53-56-696Z/internet-transport-result.json), [reproduction script](../../../.artifacts/wildly-unqualified/internet-test-2026-09-05T20-53-56-696Z/transport-smoke.mjs), [shared JPEG](../../../.artifacts/wildly-unqualified/internet-test-2026-09-05T20-53-56-696Z/internet-shared-photo.jpg) and [shared album screenshot](../../../.artifacts/wildly-unqualified/internet-test-2026-09-05T20-53-56-696Z/internet-shared-album.png). The test server saved and exited 0; its world and evidence were preserved. No invitations were sent.

At initial handoff the room was paused with zero reserved slots, and readiness, public JavaScript SHA-256 and empty slots were independently verified. **The later visible playtest supersedes that room state:** four player slots and earned photos are now preserved, and the room was restored paused after reconnect. The private [internet host access file](../../../.artifacts/wildly-unqualified/INTERNET-HOST-ACCESS.md) contains the host recovery link; the host can copy a separate guest invite in Settings and reassign reserved guest slots. The secret-free [launch record](../../../.artifacts/wildly-unqualified/internet-launch.json) tracks the changed origin and process configuration.

The internet server was rechecked listening on `127.0.0.1:4312` and the separate local server on `127.0.0.1:4310` after the visible browser cleanup. Private internet saves remain in `games/.artifacts/wildly-unqualified/internet-play-data-2026-09-05T20-58-36-319Z`; the local room uses `games/.artifacts/wildly-unqualified/local-data`. Keep the relevant game process and tunnel running; the temporary public URL can change on tunnel restart. Use the complete chosen private data directory when moving the server. No home-server access was attempted.

## Launch review events

Automatic approval review initially rejected the tunnel launch, detached local server startup and removal of the installed smoke release's generated dependencies, each stating **“blocked by policy”** without a more specific reason. Rejected actions were not retried through an obfuscated command. After the user renewed launch authorization, the detached launch remained denied; a safer normal task-managed server process and task-managed Quick Tunnel were allowed. The smoke installation was retained, and a separate clean delivery was created and hash-verified.

The local server is currently attached to the task's execution session, with output available there, rather than detached. Its private state is `games/.artifacts/wildly-unqualified/local-data`. Use the game's Save and stop control before restarting or transferring it. The earlier saved/stopped status is superseded by the successful task-managed launch.
